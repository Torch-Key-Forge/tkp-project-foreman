[project]
name = "project-foreman"
version = "0.2.0"
description = "Recover traceable project state from long AI conversations."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
